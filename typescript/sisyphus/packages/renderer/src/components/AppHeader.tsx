import React from 'react';
export default function AppHeader() {
  return (
    <header className="app-header">
      <h1>Sisyphus IDE</h1>
      {/* Add theme switch / config button etc */}
    </header>
  );
}